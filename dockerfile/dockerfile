FROM docker.io/library/debian:latest
# 安装SSH服务

ADD build_ite.sh /tmp
RUN chmod +x /tmp/build_ite.sh && /tmp/build_ite.sh
RUN apt-get clean && rm -rf /var/lib/apt/lists/* && rm -rf /tmp/*
EXPOSE 22
CMD ["/usr/sbin/sshd", "-D"]

ENTRYPOINT ["/init"]


